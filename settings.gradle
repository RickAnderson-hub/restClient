plugins {
    // Auto-provisions the JDK 21 toolchain if it isn't installed locally
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'rest-client'

include 'core'
include 'persistence'
include 'service'
include 'ui'
include 'app'
